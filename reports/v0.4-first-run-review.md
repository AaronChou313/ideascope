# v0.4 首轮端到端审查

| 环节 | 当前证据 | 结论 |
| --- | --- | --- |
| 模糊想法与范围 | demo workspace 将假设、限制与开放问题分开 | 演示夹具通过；不是模型生成结果 |
| 文献检索 | localhost Chromium 真实请求 OpenAlex 并归一化 10 条记录 | 已验证公开检索；Pages origin 待部署后验证 |
| 单智能体控制 | mock Provider 覆盖结构化、JSON 修复、检索、取消/失败与预算 | 控制流通过；真实 Provider 待凭证与授权 |
| 证据约束 | 未知 Evidence ID、空 sourced、非法图操作全量拒绝 | 自动测试通过 |
| 原子提交 | checkpoint、branch、receipt、run summary 同事务写入 | fake IndexedDB 故障测试通过 |
| 中断恢复 | 遗留 running 记录恢复为 interrupted，未提交结果不落图 | 自动测试通过 |
| 来源定位 | sourced 判断展示文献、立场、证据深度与 locator | UI/e2e 与截图复核通过 |
| 全文边界 | demo 仅标记 metadata/abstract/user excerpt，未声称读过全文 | 通过 |
| usage | 仅接受 Provider reported tokens；缺失为 unknown；不估金额 | transport 与存储测试通过 |

## 尚未通过的外部门禁

- 未调用真实或付费模型，因此模型质量、真实 token 回报、流式中断与 Provider CORS 未验证。
- 未执行 GitHub Pages 部署，因此 Pages HTTPS origin 上的 Provider/OpenAlex 行为未验证。
- 这些项目保持“待验证”，不由 mock、fixture 或构建成功替代。
