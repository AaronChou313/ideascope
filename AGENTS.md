# IdeaScope · 本地开发智能体约束

## 先读

README.md → docs/PROGRESS.md → 当前阶段路线 → 相关专题/契约。完整计划是 PROJECT_PLAN_FULL.md；日常不要每轮全读。

## 产品不可变边界

纯前端静态应用，GitHub Pages 部署；单个研究智能体；用户配置 Provider；只做文献探索、结构化理解和方向收敛。不要加入强制后端、多智能体流水线、实验执行、论文写作、账号、云同步、向量数据库或自动后台任务。

## 当前任务

首次从 v0.1.0-A 开始。一次完成一个阶段，不提前实现后续版本。用户没有要求远端操作时，不新建远端仓库、不推送、不发布。先检查并保护已有文件与未提交修改。

## 技术基线

React / TypeScript / Vite；CSS Modules + CSS tokens；React Flow + elkjs；Zustand 管 UI 临时状态；Dexie 管本地业务数据；Zod/JSON Schema 校验。一个工程，不擅自 monorepo。依赖实际安装后锁定；不凭旧知识写未经验证版本。

## 数据与 Agent

文献记录、证据片段、研究判断分离。模型不能制造已有 Paper/Evidence。图补丁经验证、revision 检查和事务提交。所有 sourced 判断必须有真实可定位且适用的依据；推断/假设/用户笔记保持标识。分支隔离，用户锁定内容不被自动改写。

## 安全与真实性

key 默认仅内存；不写源码、VITE_*、数据库、日志、prompt、导出。只向用户明确配置 origin 发对应凭证。禁止任意 URL 工具、eval、HTML 执行、公共 CORS 代理。mock 与真实状态始终分明。没有凭证/联网/部署验证时明确写待验证，不能谎报测试。

## 视觉

按 design/index.html 与 docs/05_DESIGN_SYSTEM.md 实现。视觉基线为黑/白/中性灰，蓝色仅用于主操作、当前焦点与选中关系；普通面板、侧栏和研究节点保持白底或极浅中性灰，不使用大面积淡绿、淡蓝或淡紫背景。整体参考成熟开发工具的克制感，而不是典型 AI SaaS。保持可读节点、中央地图、紧凑导航与右侧对话。不要加入渐变、粒子特效、巨大彩色卡片或流程编排器视觉。原型不是生产代码，按组件与领域模型重做实现。

## 完成一个阶段

跑相关 lint/typecheck/unit/contract/build/e2e；UI 阶段必须截图检查。补充测试、更新 docs/PROGRESS.md 与变更说明，报告完成项、真实测试结果、阻断与下一阶段。不要把“代码已写”当作“功能验收通过”。
